
<ul>
  <% @restaurants.each do |restaurant|%>
    <li>
      <h2>
        <%= link_to restaurant.name, restaurant_path(restaurant.id)%>
      </h2>
      <p><%= restaurant.address %><span> </span><%= restaurant.category %></p>
    </li>
  <% end %>
</ul>
